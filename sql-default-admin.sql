-- Default admin is created automatically on first backend startup
-- (see AdminDataInitializer.java).
--
-- Login credentials:
--   Email: kumarinagadurgak@gmail.com
--   Password: kumari
--   Role: admin
--
-- If you already have a plain-text admin password in DB from older code,
-- delete that row and restart the backend once to recreate BCrypt password.

USE cafesystem;
